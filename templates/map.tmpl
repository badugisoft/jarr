// ********************************************************* //
// The content of this file is auto-generated, DO NOT MODIFY //
// ********************************************************* //

package jarr

import "reflect"

{{range .Types}}
func Map{{.Name}}(slice interface{}, f func(e interface{}) {{.Type}}) []{{.Type}} {
	v := reflect.ValueOf(slice)
	ret := make([]{{.Type}}, v.Len())
	for i := 0; i < v.Len(); i++ {
		ret[i] = f(v.Index(i).Addr().Interface())
	}
	return ret
}

func Map{{.Name}}Index(slice interface{}, f func(i int, e interface{}) {{.Type}}) []{{.Type}} {
	v := reflect.ValueOf(slice)
	ret := make([]{{.Type}}, v.Len())
	for i := 0; i < v.Len(); i++ {
		ret[i] = f(i, v.Index(i).Addr().Interface())
	}
	return ret
}
{{end}}
